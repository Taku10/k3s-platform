apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: fairshare
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod

spec:
  ingressClassName: traefik

  tls:
    - hosts:
        - fairshare.takunda.cloud
      secretName: fairshare-tls

  rules:
    - host: fairshare.takunda.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fairshare-client
                port:
                  number: 80