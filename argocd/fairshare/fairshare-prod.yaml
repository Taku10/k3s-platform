apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: fairshare-prod
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: fairshare:prod
  labels:
    app.kubernetes.io/name: fairshare
    app.kubernetes.io/instance: fairshare-prod
    app.kubernetes.io/part-of: fairshare
    platform.takunda.cloud/environment: prod
    platform.takunda.cloud/category: workload

spec:
  project: fairshare

  source:
    repoURL: https://github.com/Taku10/k3s-platform.git
    targetRevision: stage/prod
    path: .

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
