apiVersion: v1
kind: Service
metadata:
  name: fairshare-server
  namespace: nonprod
spec:
  ports:
  - name: http
    port: 5000
    targetPort: 5000
  selector:
    app: fairshare-server
  type: ClusterIP
